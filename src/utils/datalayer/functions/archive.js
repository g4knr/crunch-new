// /**
//  * submits a given form
//  * @param {element} form
//  */

// const formSubmitEvent = (form) => {
//   const formSubmitEvent = {
//     event: 'form_submit',
//     formType: formType(form),
//     formName: formName(form),
//     pageURL: pageURL(),
//     formStepNumber: formTotalSteps(form),
//   };

//   let formProduct = productName(form);
//   if (formProduct) formSubmitEvent.productName = formProduct;

//   crunchDefs.functions.pushToDataLayer(formSubmitEvent);
// };
